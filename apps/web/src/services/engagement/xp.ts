/**
 * XP weights for the "+XP" celebrations right after an action. The rules themselves (and
 * everything derived from them) live in @suffa/engagement, shared with the server.
 */
export { HEARD_THRESHOLD, XP_RULES } from '@suffa/engagement';
