name: release

# Builds the suffa-web and suffa-api images, publishes them to GHCR and (optionally) deploys
# them to CapRover. Mirrors Tabayyun's release workflow.
# The feature branch is included until it is merged, so images exist for the first deploy.
# TODO(2026-10-05): remove the feature branch trigger once it is merged into main.
on:
  push:
    branches: [main, claude/ai-tutor-platform-spec-f7u0ar]
  workflow_dispatch:

permissions:
  contents: read

env:
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/suffa

jobs:
  images:
    name: build and publish ${{ matrix.name }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    strategy:
      matrix:
        include:
          - name: web
            dockerfile: infra/docker/web.Dockerfile
          - name: api
            dockerfile: infra/docker/api.Dockerfile
    steps:
      - uses: actions/checkout@v4
      - id: tag
        run: echo "tag=sha-$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-${{ matrix.name }}:${{ steps.tag.outputs.tag }}
            ${{ env.IMAGE_PREFIX }}-${{ matrix.name }}:latest
          # web: public values compiled into the PWA (anon key is protected by RLS); empty = offline build.
          # api: version string reported by /api/version.
          build-args: |
            VITE_SUPABASE_URL=${{ vars.VITE_SUPABASE_URL }}
            VITE_SUPABASE_ANON_KEY=${{ vars.VITE_SUPABASE_ANON_KEY }}
            VITE_SYNC_ENABLED=${{ vars.VITE_SYNC_ENABLED || 'true' }}
            SUFFA_VERSION=${{ steps.tag.outputs.tag }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}

  deploy-caprover:
    name: deploy to CapRover
    needs: images
    runs-on: ubuntu-latest
    # Skipped until the repository variable CAPROVER_SERVER is set.
    if: ${{ vars.CAPROVER_SERVER != '' }}
    steps:
      - name: which apps are configured?
        id: gate
        env:
          API_TOKEN: ${{ secrets.CAPROVER_APP_TOKEN_API }}
          WORKER_TOKEN: ${{ secrets.CAPROVER_APP_TOKEN_WORKER }}
        run: |
          echo "api=$([ -n "$API_TOKEN" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
          echo "worker=$([ -n "$WORKER_TOKEN" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"
      # Order matters: the api migrates the schema; the worker waits for it (exit code 3).
      - name: deploy api
        if: steps.gate.outputs.api == 'true'
        uses: caprover/deploy-from-github@v2
        with:
          server: ${{ vars.CAPROVER_SERVER }}
          app: ${{ vars.CAPROVER_APP_API || 'suffa-api' }}
          token: ${{ secrets.CAPROVER_APP_TOKEN_API }}
          image: ${{ env.IMAGE_PREFIX }}-api:${{ needs.images.outputs.tag }}
      - name: deploy worker
        if: steps.gate.outputs.worker == 'true'
        uses: caprover/deploy-from-github@v2
        with:
          server: ${{ vars.CAPROVER_SERVER }}
          app: ${{ vars.CAPROVER_APP_WORKER || 'suffa-worker' }}
          token: ${{ secrets.CAPROVER_APP_TOKEN_WORKER }}
          image: ${{ env.IMAGE_PREFIX }}-api:${{ needs.images.outputs.tag }}
      - name: deploy web
        uses: caprover/deploy-from-github@v2
        with:
          server: ${{ vars.CAPROVER_SERVER }}
          app: ${{ vars.CAPROVER_APP_WEB || 'suffa-web' }}
          token: ${{ secrets.CAPROVER_APP_TOKEN_WEB }}
          image: ${{ env.IMAGE_PREFIX }}-web:${{ needs.images.outputs.tag }}
