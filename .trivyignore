# Accepted findings of the release image scan (Trivy, .github/workflows/release.yml).
# One CVE id per line, each with the reason and a date to look again, e.g.
#   CVE-2025-00000  # in code the image never runs; revisit 2026-12-01
# The scan stops only on critical findings that already have a fix.
